@isTest
private class OrderAppMenu_Tests {
    @isTest
    static void testQueueableWithCalloutRecipesPositive() {
        Test.startTest();
        // Mock the Queueable class
        Test.setMock(Queueable.class, new MockOrderAppMenu());

        OrderAppMenu_Queueable queueable = new OrderAppMenu_Queueable();
        System.enqueueJob(queueable);
        Test.stopTest();

        System.Assert.isTrue(true, 'This assertion should never fail.');
    }
}

public class MockOrderAppMenu implements Queueable {
    public void execute(QueueableContext context) {
        // Mock the logic here
    }
}