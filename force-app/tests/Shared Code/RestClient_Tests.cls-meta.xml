@isTest
public class TestDataHelpersTest {

    @isTest
    static void testCreateAccount() {
        TestDataHelpers.createAccount(true, 'US');
        // Perform assertions or verifications if needed
    }

    @isTest
    static void testGenXNumberOfAccounts() {
        List<Account> accounts = TestDataHelpers.genXNumberOfAccounts(5);
        // Perform assertions or verifications if needed
        System.assertEquals(5, accounts.size());
    }

    @isTest
    static void testGenAccountWithOptions() {
        Account account = TestDataHelpers.genAccountWithOptions(false, null);
        // Perform assertions or verifications if needed
        System.assertNotEquals(null, account);
    }

    @isTest
    static void testGenContactForAccount() {
        Account testAccount = new Account(Name = 'Test Account');
        insert testAccount;
        
        Contact contact = TestDataHelpers.genContactForAccount(testAccount.Id);
        // Perform assertions or verifications if needed
        System.assertEquals(testAccount.Id, contact.AccountId);
    }
}